# URL base da API. Em dev, deixe vazio para usar o proxy do Vite (/api).
VITE_API_BASE=
# API key que o front envia ao backend (header x-api-key).
# Deve bater com APP_API_KEY do backend.
VITE_APP_API_KEY=troque-esta-chave-em-producao
